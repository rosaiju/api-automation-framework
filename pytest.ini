[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    smoke: Fast smoke tests covering core happy paths
    regression: Full regression suite
    negative: Negative/edge case tests
    schema: Schema validation tests
